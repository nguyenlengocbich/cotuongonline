import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nguyenlengocbich.cotuong',
  appName: 'Cờ Tướng Online',
  webDir: 'dist'
};

export default config;
